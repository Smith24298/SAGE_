# This acts as a placeholder if vector retrieval (e.g. Pinecone) is needed long term.
# Currently the system relies on MongoDB context ingestion for RAG.
class VectorStore:
    def __init__(self):
        pass
